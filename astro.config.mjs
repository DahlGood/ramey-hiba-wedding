import { defineConfig } from 'astro/config';

// When you set up a custom domain, change `site` to that domain (e.g. 'https://rameyandhiba.com')
// and set `base` to '/'.
export default defineConfig({
  site: 'https://dahlgood.github.io',
  base: '/ramey-hiba-wedding',
  trailingSlash: 'ignore',
});
